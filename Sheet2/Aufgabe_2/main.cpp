/* * Aufgabe 5 - Biggest Product * Michel Kugler - 2.09.2017 * * In dieser Aufgabe soll eine Methode implementiert werden, welche als Paramter * einen Array mit Integer Werten enthält. Aus diesem Array soll nun das grösste * Produkt gefunden werden, welches mit 3 Elementen aus diesem Array erstellt * werden kann. * * class ArrayUtil { * public: * static int highestProduct(int *values, const int ARRAY_SIZE); * }; * * * Issues solved in ArrayUtil2.h */#include <iostream>#include <ctime>#include "../myFunctions/functions.h"#include "ArrayUtil2.h"using namespace std;int main() {    const int SIZE = 10;    // Array with 10, for special tests    // int stockValues[]{0,0,0,0,0,0,-2,-1,2,-3};    // Create Array to test and fill with numbers between    // SIZE*-1+maxNumber and maxNumber    // to test if it works with negative numbers as well    int stockValues[SIZE];    int maxNumber = 2;    for (int i = 0; i < SIZE; ++i) {        stockValues[i] = i * -1 + maxNumber;        cout << " Array[" << i << "] => " << i*-1+maxNumber << endl;    }    // To print the Array    //print_r(stockValues, SIZE);    clock_t start, stop;    start = clock();    cout << "Result = " << ArrayUtil2::highestProduct(stockValues, SIZE) << endl << endl;    stop = clock();    // Print the runtime. Needs only Start Value (stock value has Default clock() as value    print_runtime(start, stop, SIZE);    return 0;}